# 复制为 .env 后按需修改，docker compose 会读取
# cp .system/.env.example .system/.env

# ── 镜像版本 ──────────────────────────────────────────
# 构建时写入镜像标签，也可命令行覆盖：VERSION=1.0.0 docker compose up -d --build
VERSION=latest

# ── Postgres ────────────────────────────────────────────
POSTGRES_USER=dev
POSTGRES_PASSWORD=dev
POSTGRES_DB=app

# ── Sisyphus (LLM) ───────────────────────────────────────
# Anthropic（Claude）
ANTHROPIC_API_KEY=
# 可选：自定义 API 地址（代理、自建网关等）
ANTHROPIC_BASE_URL=
# 可选：默认模型（如 claude-sonnet-4-20250514、claude-3-5-haiku-20241022）
ANTHROPIC_MODEL=

# OpenAI 或兼容接口
OPENAI_API_KEY=
# 可选：自定义 API 地址（Azure、代理、本地模型等）
OPENAI_BASE_URL=
# 可选：默认模型（如 gpt-4o、gpt-4o-mini）
OPENAI_MODEL=

# 通用：若网关支持，可覆盖默认模型（如 anthropic/claude-sonnet-4-20250514、openai/gpt-4o）
LLM_MODEL=

# ── OpenClaw Gateway ───────────────────────────────────
# Gateway 认证 token（可选，建议生产环境设置）
OPENCLAW_GATEWAY_TOKEN=

# ── 飞书 (Feishu) Channel ──────────────────────────────
# 在飞书开放平台创建应用后获取
# 详见 docs/FEISHU-CHANNEL-SETUP.md
FEISHU_APP_ID=
FEISHU_APP_SECRET=
