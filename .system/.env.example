# 复制为 .env 后按需修改，docker compose 会读取
# cp .system/.env.example .system/.env

# ── 挂载根目录 ────────────────────────────────────────
# 容器数据目录的宿主机根路径，下面会创建 workspace_data、openclaw_data、postgres_data 子目录
MOUNT_ROOT=

# ── 镜像版本 ──────────────────────────────────────────
VERSION=latest

# ── Postgres ────────────────────────────────────────────
POSTGRES_USER=dev
POSTGRES_PASSWORD=dev
POSTGRES_DB=app

# ── SSH（端口 10220）────────────────────────────────────
# 设置后可用 root + 此密码登录：ssh -p 10220 root@<主机>
ROOT_PASSWORD=
